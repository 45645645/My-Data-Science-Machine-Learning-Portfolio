# 📂 Data

This folder stores datasets used for anomaly detection experiments.

You may include:

- Synthetic time-series data  
- Anonymized sensor / equipment logs  
- CSV files exported from real systems (with sensitive fields removed)

Example files (planned):

- `synthetic_timeseries.csv`
- `equipment_log_sample.csv`
